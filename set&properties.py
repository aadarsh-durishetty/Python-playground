# set
# set can have mixed data types
# element cannot be repeated, if repeated it prints once  

s = {2,'cat',45.8,'crystal','floods',2,2,2,2} # when an element repeated only prints once
print(s)

# cannot print wrt index as the order is not defined and taken as random

print(type(s)) # data type